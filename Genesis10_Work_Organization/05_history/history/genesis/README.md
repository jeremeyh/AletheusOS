# Genesis Script Archive

This directory preserves completed Genesis construction and migration scripts.
These scripts are retained for provenance, auditability, and historical reconstruction; they are not part of the active runtime package.

Run a historical script from the repository root so its original relative paths continue to resolve:

```bash
bash history/genesis/scripts/<script-name>.sh
```
