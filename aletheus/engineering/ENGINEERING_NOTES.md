# Engineering Notes — GP-00036

This package is intentionally integration-first.

It does not create a new subsystem.

It extends:
- `aletheus/repository_dna/`
- `aletheus/atlas/`

The objective is to reduce overlap by making Repository DNA the canonical inventory source and Atlas the topology interpreter.
