import {useEffect,useRef} from "react"; import type {PhaseState} from "../types";
export function AdaptiveField({phase,density}:{phase:PhaseState;density:number}){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const c=ref.current;if(!c)return;const x=c.getContext("2d");if(!x)return;let a=0,t=0;const resize=()=>{const d=Math.min(devicePixelRatio||1,2);c.width=c.clientWidth*d;c.height=c.clientHeight*d;x.setTransform(d,0,0,d,0,0)};const stiffness=phase==="Nebular"?.35:phase==="Fluid Reactive"?.55:phase==="Quasi-Crystalline"?.78:.98;const draw=()=>{t+=.008;const w=c.clientWidth,h=c.clientHeight,cx=w*.58,cy=h*.5;x.clearRect(0,0,w,h);const g=x.createRadialGradient(cx,cy,0,cx,cy,220);g.addColorStop(0,`rgba(217,184,91,${.08+stiffness*.11})`);g.addColorStop(1,"rgba(0,0,0,0)");x.fillStyle=g;x.fillRect(0,0,w,h);for(let r=0;r<6;r++){x.beginPath();x.ellipse(cx,cy,70+r*18,24+r*11,t*(r%2?-.22:.18)+r*.4,0,Math.PI*2);x.strokeStyle=r===2?`rgba(228,193,88,${.25+stiffness*.35})`:`rgba(109,155,255,${.08+density*.08})`;x.lineWidth=r===2?1.4:.8;x.stroke()}for(let i=0;i<Math.floor(36+density*64);i++){const ang=t*(.5+(i%7)*.03)+i*.61,orb=(32+(i%17)*6)*(.7+density*.5),px=cx+Math.cos(ang)*orb,py=cy+Math.sin(ang*1.31)*orb*.42;x.beginPath();x.arc(px,py,.7+(i%3)*.45,0,Math.PI*2);x.fillStyle=i%8===0?"rgba(241,205,92,.92)":"rgba(134,179,255,.48)";x.fill()}a=requestAnimationFrame(draw)};resize();addEventListener("resize",resize);draw();return()=>{cancelAnimationFrame(a);removeEventListener("resize",resize)}},[phase,density]);return <canvas ref={ref} className="adaptive-field" aria-hidden="true"/>}
