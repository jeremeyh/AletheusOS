import type { InstrumentDefinition } from "../types";
export function InstrumentCard({instrument,active,onSelect}:{instrument:InstrumentDefinition;active:boolean;onSelect:()=>void}){return <button className={`instrument-card ${active?"active":""} risk-${instrument.risk}`} onClick={onSelect} style={{"--accent":instrument.accent} as React.CSSProperties}><div className="instrument-card__head"><span className="signal-dot"/><span>{instrument.metric}</span><img src="/aletheusos-logo.jpeg" alt=""/></div><strong>{instrument.value.toFixed(1)}</strong><h3>{instrument.title}</h3><p>{instrument.detail}</p></button>}
