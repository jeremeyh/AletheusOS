import {useEffect,useRef} from "react";export function OrbitalField({intensity}:{intensity:number}){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const c=ref.current;if(!c)return;const x=c.getContext("2d");if(!x)return;let f=0,id=0;const resize=()=>{const d=Math.min(devicePixelRatio||1,2);c.width=c.clientWidth*d;c.height=c.clientHeight*d;x.setTransform(d,0,0,d,0,0)};const draw=()=>{f+=.008;const w=c.clientWidth,h=c.clientHeight,cx=w/2,cy=h*.48,r=Math.min(w,h)*.22;x.clearRect(0,0,w,h);const g=x.createRadialGradient(cx,cy,0,cx,cy,r*1.8);g.addColorStop(0,`rgba(212,175,55,${.18+intensity*.15})`);g.addColorStop(.35,"rgba(70,140,255,.08)");g.addColorStop(1,"transparent");x.fillStyle=g;x.fillRect(0,0,w,h);for(let k=0;k<6;k++){x.beginPath();x.ellipse(cx,cy,r*(.45+k*.18),r*(.22+k*.095),f*(k%2===0?.15:-.1)+k*.32,0,Math.PI*2);x.strokeStyle=k===2?"rgba(212,175,55,.55)":`rgba(124,168,255,${.08+k*.02})`;x.lineWidth=k===2?1.4:.8;x.stroke()}for(let i=0;i<56;i++){const a=f*(.35+(i%5)*.03)+i*.61,o=r*(.35+(i%11)*.08);x.beginPath();x.arc(cx+Math.cos(a)*o,cy+Math.sin(a*1.27)*o*.48,.8+(i%4)*.45,0,Math.PI*2);x.fillStyle=i%7===0?"rgba(240,211,124,.9)":"rgba(147,187,255,.45)";x.fill()}id=requestAnimationFrame(draw)};resize();addEventListener("resize",resize);draw();return()=>{cancelAnimationFrame(id);removeEventListener("resize",resize)}},[intensity]);return <canvas className="orbital" ref={ref}/> }