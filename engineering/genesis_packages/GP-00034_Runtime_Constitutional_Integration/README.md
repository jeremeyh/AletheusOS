# GP-00034 — Runtime Constitutional Integration

Type: ADDITIVE_WITH_PATCH_TEMPLATE

## Merge instruction

Copy only:

```text
aletheus/runtime_constitutional_integration/
```

into your current `aletheus/` package.

Do **not** replace your existing `runtime/` folder.

## Patch note

This package includes a patch template, not an automatic runtime overwrite.
Review `patches/runtime_core_integration_example.patch` before wiring into `runtime/core.py`.
